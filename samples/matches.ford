contract Matches;

def doMatch {

    match {
        case x > 10 : {
            // do this
        }
        case x <= 10 : {
            // do this
        }
        case _ : {
            // or do that
        }
    }
}
