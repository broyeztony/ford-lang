- automatically add `receive` and `fallback` function to the contract
- add yaml specification companion file (and automatically update it on code changes)
- build a playground UI website
- remove `while` and `do while` iteration contructs
- handle mappings
- handle lists
- provide syntactic sugar for list's searchs, removals, insertions
