{
  "name": "ford",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "clear && node src/controller.js playground.ford playground.spec.yaml > compilation_artifacts/playground.sol && cat playground.spec.yaml && cat compilation_artifacts/playground.sol && solcjs compilation_artifacts/playground.sol --abi -o compilation_artifacts && cat compilation_artifacts/compilation_artifacts_playground_sol_Playground.abi"
  },
  "dependencies": {
    "solc-typed-ast": "^18.1.6",
    "yaml": "^2.4.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/broyeztony/ford.git"
  },
  "private": true,
  "devDependencies": {
    "solc": "^0.8.26"
  }
}
